# Override settings in _config.yml here
baseurl: "" # the subpath of your site, e.g. /blog/

# Assets pipeline settings
assets:
  css_compressor: 
  js_compressor: 
  cache: false
  cachebust: none
  debug: true
# We're using Jekyll < 2.0.0 which defaults the necoding to nil
encoding: utf-8
